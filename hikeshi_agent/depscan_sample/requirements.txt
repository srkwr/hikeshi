# 【合成サンプル manifest】防火（依存脆弱性スキャン）デモ用。
# これは実プロジェクトの依存ではなく、既知 CVE を引くために意図的に古い版を固定した合成データ。
# 返ってくる脆弱性アドバイザリ（GHSA/CVE/PYSEC）は OSV.dev の【実物】です（§20）。
jinja2==2.4.1
requests==2.19.1
urllib3==1.24.1
pyyaml==5.1
flask==0.12.2
